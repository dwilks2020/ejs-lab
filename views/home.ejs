<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= RESTAURANT.name %></title>
</head>
<body>
    <%- include('./partials/nav') %>

    <h1>Welcome to <%= RESTAURANT.name%></h1>
    <ul> <%= RESTAURANT.name%></ul>   
    <ul> Address: <%= RESTAURANT.address %></ul>
    <ul> Phone:  <%= RESTAURANT.phone %> </ul>

    <% if (RESTAURANT.isOpen) { %>
        <p>Status: We are open! </p>
      <% } else { %>
        <p>Status: Sorry, we are currently closed.</p>
      <% } %>
    
</body>
</html>
